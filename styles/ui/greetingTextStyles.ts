import {StyleSheet} from 'react-native';

const greetingTextStyles = StyleSheet.create({
    greetingText: {
        textAlign: 'center',
        marginBottom: 20,
    },
});

export default greetingTextStyles;