import React from 'react';
import {View} from "react-native";
import TosScreen from "@/screens/auth/TosPage";

const TosPage: React.FC = () => {
    return (
        <View style={{flex: 1}}>
            <TosScreen/>
        </View>
    );
};

export default TosPage;